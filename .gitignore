# ── Data (large / local-only) ──
# Ship layout docs only from Data-DataBento; copy parquet/CSV/.zst locally or on VPS (see docs/DATABENTO_KNOWLEDGE.md).
Data-DataBento/**
!Data-DataBento/README.md
!Data-DataBento/normalization_summary.csv
DATA/
data/raw/
data/processed/
data/ml_datasets/
outputs/
Vincere/*.csv

# ── Binary / archive artifacts ──
*.parquet
*.zip
*.7z
*.dll

# ── Submodule artifacts ──
project-root

# ── .NET (C# backtester) ──
csharp/**/bin/
csharp/**/obj/

# ── Python ──
__pycache__/
*.pyc
*.pyo
.venv/
.pytest_cache/
.env
*.egg-info/
dist/
build/

# ── OS junk ──
.DS_Store

# ── Editor / backup cruft ──
*.bak

# ── Runtime logs ──
projectx/logs/*.log

# ── Phoenix local state / exports (machine-specific) ──
projectx/.env
projectx/strategy/state/*.json
projectx/orders_export*.csv

# Generated backtest / prop / replay outputs (regenerate after clone)
reports/
