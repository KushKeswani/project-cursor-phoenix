# Templates — Deprecated

Templates are **no longer used**. All range, trade, session, and close-all times are hardcoded in the strategy for each instrument. Just add the strategy, select instrument (or use the per-instrument strategy), and set the bar size on the chart.
